# PortraitGAN — Python Dependencies
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# Image handling
Pillow>=9.0.0

# Training utilities
tqdm>=4.64.0
matplotlib>=3.6.0
pyyaml>=6.0

# Dataset download helpers
gdown>=4.7.0          # for CelebA from Google Drive

# Evaluation (optional but recommended)
scipy>=1.10.0         # for FID stats
clean-fid>=0.1.35     # FID computation

# Optional: Kaggle API for dataset download
# kaggle>=1.5.0

# Optional: faster image loading during training
# Pillow-SIMD>=9.0.0  # drop-in replacement, faster
